apply plugin: 'idea'
apply plugin: 'java'
apply plugin: 'maven'

repositories {
    mavenCentral()
}

dependencies {
    testCompile(
            [group: 'junit', name: 'junit', version: '4.11'],
            [group: 'org.assertj', name: 'assertj-core', version: '3.0.0']
    )
}
